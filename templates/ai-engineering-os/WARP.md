# Warp Operating Rules

> Thin pointer. Canonical AI policy lives in [`CLAUDE.md`](./CLAUDE.md).

---

## Hard rules

1. **Read `CLAUDE.md` first.** Source of truth for identity, governance, hooks, MCP gates, skills.
2. **Do not bypass** Claude Code hooks, MCP governance, tests, or validation.
3. **Use Warp as the terminal cockpit only.** Layout, panes, command palette, visual diffs.
4. **Use Claude Code as the governed execution path.**
5. **Use SocratiCode (when installed)** for codebase intelligence before broad code exploration.
6. **Do not use Warp cloud agents** on this repo unless explicitly approved.
7. **Do not duplicate canonical policy here.**

See `docs/WARP_COCKPIT.md` for layout and privacy checklist; `docs/WARP_WORKFLOWS.md` for snippets.
